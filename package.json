{
  "name": "dex",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "dev": "bun run server.ts",
    "start": "bun run dist/server.js",
    "build": "bun build server.ts --outfile dist/server.js --target bun"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}